import React from 'react';
import styled from 'styled-components';

export default styled.div`
    position: absolute;
    z-index: 10;
    top: 10px;
    right: 10px;
    color: #fff;
    cursor: pointer;
`